////////////////////////////////////////////////////////
		// if (current_cmd->index == (curr_prompt->args_amount - 1) &&
		// (ft_strncmp(current_cmd->cmd_line, "ls", ft_strlen(current_cmd->cmd_line)) == 0))
		// {
		// 	t_cmd *tmp;
		// 	int	is_cat_sequence = 0;
		// 	int cat = 0;
			
		// 	tmp = curr_prompt->cmds;
		// 	while (tmp->next != NULL)
		// 	{
		// 		if (ft_strncmp(tmp->cmd_line, "cat", ft_strlen(tmp->cmd_line)) == 0)
		// 			is_cat_sequence = 1;
		// 		if (is_cat_sequence)
		// 		{
		// 			while (tmp->next != NULL && 
		// 			ft_strncmp(tmp->cmd_line, "cat", ft_strlen(tmp->cmd_line)) == 0)
		// 			{
		// 				cat++;						
		// 				tmp = tmp->next;
		// 			}
		// 			is_cat_sequence = 0;
		// 			break;					
		// 		}
		// 		tmp = tmp->next;
		// 	}
			
		// 	char *res;
		// 	char *prompt;
			
		// 	prompt = NULL;
		// 	res = NULL;
		// 	while (cat > 0)
		// 	{
		// 		res = get_next_line(0);
		// 		cat--;	
		// 	}
			
		// 	ft_free_ptr((void *) &res);			
		// 	ft_free_ptr((void *) &prompt);
		
		// }
		//////////////////////////////////////////////////////