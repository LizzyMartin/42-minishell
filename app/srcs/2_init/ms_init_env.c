#include <minishell.h>

void	init_env(t_ms *ms)
{
	// int		i;
	// char	**envp;
	// char	**envp_splitted;

	// i = 0;
	// envp = ms->envp;
	// while (envp[i])
	// {
	// 	envp_splitted = ft_split(envp[i], "=");
	// 	// ms->p.envs.key = envp_splitted[0];
	// 	// ms->p.envs.key = envp_splitted[0];
	// 	// ms->p.envs = ms->p.envs.next;
	// 	i++;
	// }
    if (0)
    {
        printf("%d", ms->argc);
    }

}
